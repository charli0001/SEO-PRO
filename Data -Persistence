function saveToStorage(key, value) {
    localStorage.setItem(`seoMaster_${key}`, JSON.stringify(value));
}

function getFromStorage(key) {
    const item = localStorage.getItem(`seoMaster_${key}`);
    return item ? JSON.parse(item) : null;
}
